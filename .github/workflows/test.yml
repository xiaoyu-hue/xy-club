name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  unit:
    name: Unit (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    # 防挂保护：去掉了 Node 18 不支持的 --test-timeout 后，用作业级超时兜底，
    # 避免某个用例意外卡死把整个 CI 跑成无期限挂起。对所有 Node 版本都生效。
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        node-version: ['18', '20', '22']
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 准备 Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      # 仓库里没有 package-lock.json（依赖只有一个 express），用 install 而非 ci
      - name: 安装依赖
        run: npm install --no-audit --no-fund

      - name: 运行单元测试
        run: npm test

# 说明：E2E 不进 CI。它需要下载 Chromium（数分钟），而单元测试已经覆盖了
# 鉴权 / 上传 / 数据完整性这些真正有风险的层面。E2E 留作本地发布前验证，
# 见 docs/TESTING.md。
